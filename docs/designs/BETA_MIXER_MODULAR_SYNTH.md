---
status: ACTIVE
supersedes: 2026-04-30-alpha2-descriptor-oscilloscope.md (α2 — PROMOTED, shipped)
---
# CEO Plan: StimDAW β — Mixer + Modular Synth (Reason-style)
Generated by /plan-ceo-review on 2026-05-01
Branch: main | Mode: SELECTIVE EXPANSION
Repo: froling/StimDAW

## Vision (12 months)

Stim som live-instrument, inte ett verktyg där man redigerar JSON-filer. β-wedgen
är **modular synthesizer** med stim-output istället för audio: LFOs som genererar
rörelse, mixer som routar signaler till elcon-par, patch-cables som binder ihop.

α2 gav "se vad boxen genererar". β ger "spela boxen som ett instrument".

### 10x Check

Inte "pattern editor" (ursprunglig TODOS.md-framing). Det är **modular synth-paradigmet**
applicerat på stim. Reason/Eurorack mental-modell: oscillatorer modulerar parametrar
via patch-cables, knobbar är base-värden som signalen swingar runt.

### Platonic Ideal

```
┌──────────────────────────────────────────────────────────────────┐
│                       Mixer panel                                 │
│                                                                   │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐        │
│  │  AC↔BD       │    │  BD↔AC       │    │  +channel    │        │
│  │  pulse_width │◀═  │  pulse_width │◀═  │              │        │
│  │  ⊙ 144µs     │ ║  │  ⊙ 144µs     │ ║  │              │        │
│  │  pace        │ ║  │  pace        │ ║  │              │        │
│  │  ⊙ 25ms      │ ║  │  ⊙ 7ms       │ ║  │              │        │
│  │  amplitude   │ ║  │  amplitude   │ ║  │              │        │
│  │  ⊙ 50%       │ ║  │  ⊙ 50%       │ ║  │              │        │
│  └──────────────┘ ║  └──────────────┘ ║  └──────────────┘        │
│                   ║                   ║                           │
│         ╔═════════╝         ╔═════════╝                           │
│         ║  bezier cables    ║                                     │
│         ║  (visa routing)   ║                                     │
│         ▼                   ▼                                     │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐        │
│  │ LFO 1        │    │ LFO 2        │    │ +LFO         │        │
│  │ ∿ sine       │    │ /| sawtooth  │    │              │        │
│  │ rate ⊙ 0.5Hz │    │ rate ⊙ 2Hz   │    │              │        │
│  │ amount ⊙ 80% │    │ amount ⊙ 30% │    │              │        │
│  │ ●→ output    │    │ ●→ output    │    │              │        │
│  └──────────────┘    └──────────────┘    └──────────────┘        │
└──────────────────────────────────────────────────────────────────┘
                              │
                              ▼  effective_pulse_width = base + signal(t) × depth
                  ┌──────────────────────────┐
                  │   Synth Engine (tick)    │
                  │   per descriptor:        │
                  │   1. eval LFOs(t)        │
                  │   2. apply via cables    │
                  │   3. clamp hw bounds     │
                  │   4. emit PtDescriptor   │
                  └──────────────────────────┘
                              │
                              ▼
                       NeoDKClient.writePtDescriptor
                              │
                              ▼
                  Mock firmware OR real NeoDK
                              │
                              ▼
                       Oscilloscope (befintlig, visar resultatet)
```

User-felt: "rate-knob går snabbare → pulserna kommer närmare; LFO på pulse_width
gör att intensiteten andas; två LFOs på olika kanaler skapar polyrhythm."
Inget editing av JSON. Inget kod-ändring. Spela.

## Premise locks

- **Reason-style modulation:** Knob-position rör sig inte visuellt under LFO-mod.
  Knob = user-set base. LFO oscillerar runt det. UI kan visa halo/ring för
  swing-range (γ).
- **Per-channel pace = per-channel emit-rate.** Varje channel är en "egen klocka"
  som tickar enligt sin (LFO-modulerade) pace. Channels interleave på wire.
- **nrOfPulses=1 i mixer-mode.** All bursting via host-streaming, inte
  firmware-side pulse-grupper. Matchar user-clarification.
- **Pure-TS DSP.** Inga Web Audio off-label. Egna sine/saw/square-formler,
  bun-test:bara.
- **Hardware bounds clampas alltid:** pulse_width 2..200µs, pace 5..62.5ms,
  amp 0..255. Single chokepoint i synth-engine.
- **Befintlig safety-stack inerverbar:** ramp-controller + max-ceiling + STOP
  watchdog applicerar precis som i α (inga ändringar i safety-modulen).

## Datamodell — Mixer + Synth state

```typescript
// src/synth/types.ts

export interface KnobState {
  /** User-set base value, hardware-units (µs/¼ms/0..255). */
  base: number;
  /** Optional cable som modulerar denna knob; null = static. */
  modCableId: string | null;
}

export type WaveShape = 'sine' | 'saw' | 'square' | 'triangle';

export interface LFO {
  id: string;
  rate: number;        // Hz, 0.01..50
  amount: number;      // 0..1, master output gain
  shape: WaveShape;
  phase: number;       // internal radians, advances per tick
}

export interface MixerChannel {
  id: string;
  elcon: Elcon;        // [pos, neg] från befintliga types.ts
  knobs: {
    pulseWidth: KnobState;   // base 2..200µs
    pace: KnobState;          // base 5000..62500µs
    amplitude: KnobState;     // base 0..255
  };
  enabled: boolean;
  /** Internal: when next pulse emits (sim-time µs). */
  nextEmitMicros: number;
}

export interface Cable {
  id: string;
  sourceLfoId: string;
  destChannelId: string;
  destKnobName: 'pulseWidth' | 'pace' | 'amplitude';
  /** Per-cable depth 0..1. Reason-style "modulation amount" knob på cable. */
  depth: number;
}

export interface MixerState {
  channels: MixerChannel[];
  lfos: LFO[];
  cables: Cable[];
}
```

## Synth-engine — Reason-style evaluation

```typescript
// src/synth/synth-engine.ts

/** Evaluera knob: base + signal × depth × hardware-range, sedan clamp. */
function evaluateKnob(
  knob: KnobState,
  knobBounds: { min: number; max: number },
  t: number,
  state: MixerState,
): number {
  if (!knob.modCableId) return knob.base;
  const cable = state.cables.find(c => c.id === knob.modCableId);
  if (!cable) return knob.base;
  const lfo = state.lfos.find(l => l.id === cable.sourceLfoId);
  if (!lfo) return knob.base;

  const signal = computeLfoSignal(lfo, t);          // -1..+1
  const range = (knobBounds.max - knobBounds.min);
  const swing = signal * cable.depth * lfo.amount * (range / 2);
  return clamp(knob.base + swing, knobBounds.min, knobBounds.max);
}

/** Tick:as från host-side waveform-loop (eller separat 5ms-tick). */
export function tick(simNowMicros: number, state: MixerState, sink: (d: PtDescriptor) => void): void {
  for (const ch of state.channels) {
    if (!ch.enabled) continue;
    if (ch.nextEmitMicros > simNowMicros) continue;

    const pw   = evaluateKnob(ch.knobs.pulseWidth, PW_BOUNDS,   simNowMicros, state);
    const pace = evaluateKnob(ch.knobs.pace,       PACE_BOUNDS, simNowMicros, state);
    const amp  = evaluateKnob(ch.knobs.amplitude,  AMP_BOUNDS,  simNowMicros, state);

    sink({
      meta: 0,
      sequenceNumber: nextSeqNr(),
      phase: 0,                    // base phase; biphasic-flip handled by interleaving
      pulseWidthMicros: Math.round(pw),
      startTimeMicros: simNowMicros,
      electrodeSet: ch.elcon,
      nrOfPulses: 1,               // streaming model
      paceQuarterMs: Math.round(pace / 250),
      amplitude: Math.round(amp),
      deltaPulseWidthQuarters: 0,
      deltaPaceMicros: 0,
    });

    ch.nextEmitMicros = simNowMicros + Math.round(pace);
  }
}
```

## Accepted Scope Decisions

| # | Proposal | Effort | Decision | Reasoning |
|---|----------|--------|----------|-----------|
| Core | β.0 PoC: 1 channel + 1 LFO + 1 cable | M (CC ~6h) | **GRUND-SCOPE** | Smallest-viable. Validerar arkitektur innan skala-upp. |
| E1 | Multi-channel mixer | S (CC ~1h) | **ACCEPTED** | Utan multi-channel är det inget mixerbord — bara en single voice. |
| E2 | Multi-LFO support | S (CC ~1h) | **ACCEPTED** | En enda LFO blir trist; flera ger polyrhythm-känsla. |
| E3 | Patch-cable UI med bezier wires | M (CC ~3h) | **ACCEPTED** | Det här ÄR visionen — utan cables är det inte modular. |
| E6 | Per-cable depth knob | XS (CC ~30min) | **ACCEPTED** | Reason-standard. Marginalkostnad noll, signifikant uttrycks-utrymme. |
| E4 | Routing-validering (kortslutnings-skydd) | M | **DEFERRED** | Behöver design-runda — multi-channel med olika elcons kan ha komplexa kortslutnings-mönster över tid. Skippar i β.0, gör efter user-feedback. |
| E5 | Priority overrides för channel-konflikter | M | **DEFERRED** | Beroende på E4. |
| E7 | LFO sync-modes (free/tempo/one-shot) | S | **DEFERRED** | Free-mode räcker för β.0; sync-modes är γ "performance-mode". |
| E8 | Save mixer-state till .stimdaw v1 | S | **DEFERRED** | β.0 är live-jam fokus; persist är nice-to-have för γ. |

**Total β.0 scope:** PoC + E1 + E2 + E3 + E6 = **L effort (CC ~10-12h)** för full
mixer med multi-channel, multi-LFO, drag-cables, per-cable depth.

## Updated β-scope (filer)

```
src/synth/                    (NY MODUL — modular synth core)
  ├── types.ts                KnobState, LFO, MixerChannel, Cable, MixerState
  ├── waveforms.ts            sine/saw/square/triangle pure functions
  ├── lfo.ts                  computeLfoSignal(lfo, t) → -1..+1
  ├── synth-engine.ts         tick() emits PtDescriptors via sink callback
  └── state.ts                Pure data + actions (eng-review 1.2A: ej i .svelte.ts
                              så bun-test kan köra utan Svelte runtime)

src/protocol/
  └── hardware-bounds.ts      (NY per eng-review 2.2A) PULSE_WIDTH/PACE bounds från
                              firmware/inc/burst.h. Single source-of-truth — både
                              src/synth och src/ui/descriptor-timing importerar.

src/safety/
  └── clamp.ts                (NY per eng-review 2.3A) Single chokepoint clampAmp
                              flyttad från waveform.ts. Synth-engine + waveform
                              importerar härifrån (outside-voice #3 bevarat).

src/ui/synth/                 (NY MODUL — mixer UI)
  ├── synth-store.svelte.ts   Tunn Svelte 5 runes-adapter över src/synth/state.ts
  ├── knob-helpers.ts         Pure: dragDeltaToValue, log-scale konvertering
  ├── cable-helpers.ts        Pure: bezierPath(x1,y1,x2,y2) → SVG d-string
  ├── Knob.svelte             Reusable rotating knob med drag-att-justera
  ├── MixerChannel.svelte     Channel-strip: 3 knobs + cable-ports
  ├── LFOModule.svelte        LFO-panel: rate/amount/shape + output-port
  ├── CableLayer.svelte       SVG-overlay för bezier-wires (pointer-events: none,
                              path:stroke per eng-review 1.6A)
  └── Mixer.svelte            Top-level panel komponerar alla ovan

src/ui/App.svelte             (UTÖKAS) lägg till Mixer alongside Oscilloscope.
                              PatternRunnerBar och Mixer mutually-exclusive UI-state
                              (eng-review 1.3A).

test/synth/
  ├── waveforms.test.ts       ~12 cases: sine cycle = 2π, saw -1..+1 monotonic,
  │                            square ±1 boundary, triangle peaks/valleys, edge
  │                            values (phase=0, π, 2π, wrap)
  ├── lfo.test.ts             ~8 cases: phase advancement, rate change halves
  │                            period, amount=0 silences, rate=0 (DC, no NaN),
  │                            shape switching
  ├── synth-engine.test.ts    ~20 cases:
  │                            • evaluateKnob: base-only, signal=0/+1/-1, depth=0/0.5/1,
  │                              clamp under-min/over-max, depth × amount × range
  │                            • tick: emit when due, skip when not due,
  │                              GAP-A phase-flip per channel (CRITICAL 2.1A safety),
  │                              multi-channel interleave, sequenceNumber wrap at 256
  │                            • GAP-F generation pattern: stop → pending events
  │                              no-op vid fire (per 2.5A)
  │                            • bounds clamping integration
  ├── bounds-shared.test.ts   GAP-C: src/synth importerar samma konstanter som
  │                            src/ui/descriptor-timing.ts (skydda mot split)
  └── state.test.ts           ~10 cases: addChannel, addLfo, addCable mutators,
                               GAP-G cascade-delete invariants (removeLfo →
                               cables filtered, removeChannel → cables filtered),
                               mode mutex (1.3A), state-invariant: alla cables
                               pekar på existing LFOs+channels

test/ui/synth/                (pure helpers — Svelte runtime ej required)
  ├── knob-helpers.test.ts    GAP-D: ~6 cases — drag-to-value formula, Shift
  │                            fine-grain (10x sensitivity), log-scale: tToValue(0)
  │                            = PACE_MIN, tToValue(1) = PACE_MAX, tToValue(0.5)
  │                            ≈ √(MIN×MAX) (geometric mean)
  └── cable-helpers.test.ts   GAP-E: ~4 cases — bezierPath d-string format,
                               control points correctness, edge cases (port på
                               varandra, vertical line)

test/integration/
  └── mixer-flow.test.ts      ~5 cases:
                              • 1 channel + 1 LFO + 1 cable, advance through
                                full LFO period, assert pulse_width oscillates
                                expected min/max
                              • Cable removal: knob reverts till base
                              • Multi-channel different paces: emits interleave
                                korrekt (channel A pace=7ms, channel B pace=25ms)
                              • GAP-B: STOP-mid-flight (CRITICAL safety) —
                                mirror existing STOP-stuck-queue test, drain
                                pending, no late dispatches efter stop
                              • Multi-LFO independence: rate change på LFO A
                                påverkar inte cable LFO B → channel C
```

Total: ~15 nya filer (12 src + 3 test-extras), 1 utökning. **~70 nya tester
target. Project-totalt ≈ 320 tests (var 248).**

**Test gaps lyfta i eng-review** (alla in-scope):
- **GAP-A** (CRITICAL): per-channel phase-flip explicit assertion
- **GAP-B** (CRITICAL): STOP-mid-flight safety integration test
- **GAP-C**: hardware-bounds shared constant test
- **GAP-D**: knob log-scale conversion test
- **GAP-E**: cable bezier-path test
- **GAP-F**: SynthEngine generation-pattern test
- **GAP-G**: cascade-delete state-invariant test

## Temporal Interrogation (Hours 1-6+)

**HOUR 1 (foundations) — what does implementer need to know?**
- LFO phase: radians (0..2π) eller normalized (0..1)? **Decision:** radians,
  matchar matematiska sine-formler. `phase += 2π × rate × dt`.
- Cable can-only-source-LFO eller can-source-anything? **Decision:** β.0 only LFO →
  knob. Knob → knob (envelope-following) is γ.
- Knob-bounds: where? `bounds.ts` exporterar konstanter. Single chokepoint för
  hardware-clamps.

**HOUR 2-3 (core logic) — what ambiguities will hit?**
- Tick rate: per-channel-pace (varje channel egen clock) eller global tick?
  **Decision:** global tick (5ms?), per-channel `nextEmitMicros` checkar inom.
  Enklare än per-channel timer-array.
- LFO depth interaction: `cable.depth × lfo.amount × range`? Eller bara
  `cable.depth × range`? **Decision:** båda multiplicerar — `cable.depth` är
  per-routing, `lfo.amount` är master på LFOn. Två gain-stages.
- Bipolar vs unipolar LFO: sine går -1..+1, men för pace (där 0 är ogiltig) kan
  modulation push under min. **Decision:** clamp till hw-bounds (inte mod-range)
  vid sista steget. User får visualfeedback att klippet sker via Oscilloscope.

**HOUR 4-5 (integration) — what will surprise?**
- Stop button behavior: pause synth-engine eller drain queue? **Decision:**
  pause + drain queue (befintlig STOP-flow). Ingen ny safety-path.
- LFO restart vid pattern-stop? **Decision:** phase fortsätter ackumuleras
  (free-running). Reason-default. Restart kan vara γ "one-shot mode".
- Channel-add med duplicate elcon? **Decision:** tillåt — användaren kan ha
  två "voices" på samma elcon med olika modulering. UI visar varning men inte
  block.

**HOUR 6+ (polish/tests) — wishes implementer kommer ha?**
- Knob double-click → reset till sensible default. Per knob: pw=144µs,
  pace=25ms, amp=128.
- LFO solo/mute för debug. Skip i β.0, lägg som TODO.
- Visualisera LFO-signal direkt i LFO-modulen (mini-scope inside LFO-panel)?
  Nice-to-have, inte β.0.

## What already exists (leverage)

| Källa | Status |
|---|---|
| `src/protocol/descriptor.ts` | Wire-format komplett. Synth-engine outputtar PtDescriptors direkt. |
| `src/protocol/neodk-client.ts` | `writePtDescriptor()` finns. Synth-engine sink = client-write. |
| `src/safety/ramp-controller.ts` | Applicerar oavsett källa. Mixer-output går genom samma ramp. |
| `src/safety/max-ceiling.ts` | Same. Single chokepoint amp clamp i `waveform.ts` skyddar. |
| `src/safety/stop-watchdog.ts` | Same. Mixer.stop() trigger:ar samma flow. |
| `src/mock-firmware/firmware.ts` | Event-driven dispatch funkar för 1Hz-50Hz emit-rates utan ändring. |
| `src/ui/Oscilloscope.svelte` | N-rad-visualisering ger gratis feedback för mixer-output. |
| `src/patterns/types.ts` | `Elcon`, `elconId`, `isDisjoint` återanvänds i mixer-channel-validation. |
| `src/ui/stores.svelte.ts` | Svelte 5 runes-pattern; synth-store följer samma. |

**~70% av infrastructure återanvänds.** β.0 lägger på synth-graph + mixer-UI.

## Dream state delta

```
α2 (NU)                       β.0 (DENNA PLAN)            12-MONTH IDEAL
───────                       ────────────────            ──────────────
5 fasta firmware-patterns     Live mixer + LFOs +         Live-jam stim-instrument
Oscilloscope visualisering    patch-cables.               + scene-launcher (γ)
CSV-export                    Multi-channel,              + pattern-bibliotek (γ)
Loop-mode                     multi-LFO, drag-routing.    + MIDI control (γ)
                              Reason-style modulation.    + recording sessions
                              PoC → full mixer.           + multi-user "perform"
```

β.0 levererar **bryggan från 'see' till 'play'**.

## NOT in scope (deferred till TODOS.md)

| Item | Why defer |
|---|---|
| E4 Routing-kortslutnings-validering | Behöver dedicated design-runda — multi-channel med olika elcons har icke-trivial kortslutnings-mönster över tid |
| E5 Priority overrides | Beroende på E4. |
| E7 LFO sync-modes (tempo, one-shot) | Free-mode räcker för β.0. Tempo-sync förutsätter global BPM som vi inte har. |
| E8 Mixer-state persist till .stimdaw v1 | β.0 är live-jam fokus. Persist är γ "session-recording". |
| Envelope generators (ADSR) | Nice modulator-typ men LFO räcker för β.0. γ när användaren vill ha key-triggered modulation. |
| Sample-and-hold, random-walk | Specialty-modulators för γ när vanilla LFOs känns begränsade. |
| Knob halo (visualisera mod-swing) | UX-polish — knob-position stays static men halo runt visar swing-range. β.1 polish. |
| Web MIDI binding | Redan i TODOS.md från α2. β.1 eller γ. |
| Pattern-bibliotek + scene-launcher | γ work. |
| Undo/redo, keyboard shortcuts | β.1 polish. |

## Critical Risks (per Section 2-style review, compressed)

**Risk 1: Tick-rate mismatch.** Om global tick är 5ms och en channel har pace=7ms,
nästa-emit-check kan missa med upp till 5ms. **Mitigation:** Use per-channel
`nextEmitMicros` and let event-driven SimClock handle exact timing. ALTERNATIVT
ticka oftare (1ms) men mer CPU.

**Risk 2: LFO phase-drift mellan UI-display och synth-engine.** Om UI animerar
LFO-graf separat från engine, kan visa olika tid. **Mitigation:** Single source
of truth — `state.lfos[i].phase` är auktoritativt. UI läser bara, modifierar
inte.

**Risk 3: Cable-cycle (LFO → knob → ... loops).** Med bara LFO→knob (β.0) är
detta omöjligt. När vi senare lägger till knob→knob (γ), lägg cycle-detection
i routing.ts.

**Risk 4: Multi-channel kortslutning.** Channel A spelar A>B, Channel B spelar
A<B vid samma startTime → båda går till q0 (phase=0). Mock firmware queue gör
sin sak men hardware-routing kan momentärt ha alla 4 elektroder aktiva med
overlapping pos/neg. **Mitigation:** β.0 har bara 2 sub-queues (per phase),
hardware bör hantera per-burst-routing korrekt. E4 i TODOS för dedicated
analys när vi har real hardware.

**Risk 5: Drag-cable UX edge cases.** Drag cable utanför port → cancel? Drag
till samma port → swap? **Mitigation:** Decision i Hour 4 phase: drop-outside
= cancel, drop-on-occupied = replace existing, drop-on-source = no-op.

## Test plan (compressed)

**Unit (pure):**
- `waveforms.test.ts`: sine cycle, saw monotonic, square ±1, triangle linear
- `lfo.test.ts`: phase advancement, rate doubling halves period, amount=0 silences
- `synth-engine.test.ts`: knob eval (base only, with mod, depth=0/0.5/1.0),
  tick semantics (no emit before nextEmitMicros, emit when due, nextEmit
  advances by pace), bounds clamping (pw clamps to 200, pace clamps to 5..62.5ms)
- `bounds.test.ts`: clamp edge cases

**Integration:**
- `mixer-flow.test.ts`: 1 channel + 1 LFO + 1 cable, advance simNow over a
  full LFO period, assert pulse_width oscillates between expected min/max
- Cable cleanup: remove cable → channel reverts to base value
- Multi-channel: 2 channels with different paces emit at different rates

**Manual smoke:**
- UI: drag cable from LFO output to knob → see oscillation in Oscilloscope
- Add LFO, change rate → emit-rate visibly changes
- Stop button → mixer freezes, queue drains

**Test count target:** ~30-40 new tests. Total project: ~290 (var 248).

## Implementation order (suggested)

```
Phase 1: Pure synth core (Lane A, blocks all)
  1. src/synth/types.ts
  2. src/synth/waveforms.ts + tests
  3. src/synth/lfo.ts + tests
  4. src/synth/bounds.ts
  5. src/synth/synth-engine.ts + tests

Phase 2: UI primitives
  6. src/ui/synth/Knob.svelte (drag-to-rotate)
  7. src/ui/synth/synth-store.svelte.ts ($state runes adapter)
  8. src/ui/synth/MixerChannel.svelte
  9. src/ui/synth/LFOModule.svelte

Phase 3: Cable layer (most complex UI)
  10. src/ui/synth/CableLayer.svelte (SVG bezier overlay)
  11. Drag-cable interaction (mousedown port → mouseup target)

Phase 4: Composition + integration
  12. src/ui/Mixer.svelte
  13. App.svelte: lägg Mixer alongside Oscilloscope
  14. Wire synth-engine → NeoDKClient.writePtDescriptor
  15. test/integration/mixer-flow.test.ts
  16. Manual smoke + visual QA
```

Phase 1+2 kan göras klart utan UI rendering — pure data + tests. Phase 3 är
"cable wrestling" — räkna med extra tid där.

## Eng-review locks (2026-05-01)

Plan reviewed via `/plan-eng-review` 2026-05-01. All scope-impacting decisions
locked nedan. Implementation följer dessa.

**Architecture:**
- **1.1A** Tick-strategy: event-driven per-channel via `Clock`-interface.
  `clock.scheduleAt(nextEmitMicros, () => emit_and_reschedule)`. Återanvänder
  mock-firmware:s SimClock-paradigm. Real-mode: `RealtimeClock` adapter följer
  samma interface. Ingen wasted CPU, sample-perfekt timing.
- **1.2A** State-arkitektur: `src/synth/state.ts` är pure TS data + actions.
  `src/ui/synth/synth-store.svelte.ts` är tunn Svelte 5 runes-adapter. Engine
  läser direkt från pure-state. Skyddar mot bun-test-Svelte-runtime-pitfall
  (känt från α2 csv-filename-incident).
- **1.3A** Mixer/PatternRunnerBar mutually-exclusive: global "active source"
  lock. När Mixer är aktiv → PatternRunnerBar disabled (och vice versa).
  Ingen racing till `client.writePtDescriptor`.

**UI semantics:**
- **1.4A** Knob: vertikal drag (DAW-standard), Shift = fine-grain (10x
  sensitivity), **log-scale för pace** (4 dekader: 5ms..62.5ms), double-click =
  reset till default (pw=144µs, pace=25ms, amp=128).
- **1.5A** En cable per knob i β.0. Drag-drop på upptaget = replace existing.
  Multi-cable summation är γ.
- **1.6A** CableLayer SVG: `pointer-events: none` på `<svg>`, `pointer-events:
  stroke` på `<path>`. Drag-creation startar från port-DOM-elementen själva,
  inte från SVG-layer. Knobs förblir klickbara genom cables.

**Code Quality:**
- **2.1A** Per-channel polarity-flip per emit (CRITICAL safety — undviker DC-
  stim). Channel håller `lastPhase: 0|1`, alternates per emit. Phase=0
  hardcoded i tidigare draft var fel.
- **2.2A** Hardware-bounds till `src/protocol/hardware-bounds.ts` —
  single source-of-truth. Både `src/synth` och `src/ui/descriptor-timing`
  importerar (DRY).
- **2.3A** `clampAmp` flyttas till `src/safety/clamp.ts`. Synth-engine +
  waveform.ts importerar. Bevarar outside-voice #3 single chokepoint.
- **2.4A** Cascade-delete: `removeLfo(id)` + `removeChannel(id)` filtrerar
  bort relaterade cables. State-invariant: cables.every(c => sourceExists &&
  destExists).
- **2.5A** SynthEngine generation-pattern: `running: boolean` + `generation:
  number`. Schedule-callbacks captures generation, no-op vid mismatch.
  Mirror exakt mock-firmware:s `ptQueueGeneration`.
- **2.6** SeqNr private state i SynthEngine: `private seqNr: number = 0;
  nextSeqNr()` wrap at 256. Matchar `firmware.ts:txSeq`-pattern.

**Test gaps lyfta:**
- **GAP-A** (CRITICAL): Per-channel phase-flip explicit assertion i
  synth-engine.test.ts. Skyddar mot DC-regression.
- **GAP-B** (CRITICAL): STOP-mid-flight integration test i mixer-flow.test.ts.
  Mirror existing pt-descriptor-flow-STOP-stuck-queue-test.
- **GAP-C** Hardware-bounds shared constant test.
- **GAP-D** Knob log-scale conversion test (geometric mean).
- **GAP-E** Cable bezier-path test.
- **GAP-F** SynthEngine generation-pattern test.
- **GAP-G** Cascade-delete state-invariant test.

**Performance (accepted, monitor):**
- **4.2A** O(N²) `array.find`-pattern per emit accepterad för β.0 (max 10
  cables). TODO i synth-engine.ts: bench när channels > 10.
- **4.3A** Svelte 5 reactivity verifieras vid implementation; bench i Chrome
  DevTools om jank uppstår.
- **4.4A** GC från PtDescriptor-allokeringar accepteras. V8 minor GC är
  optimerad för 570 small objs/s.

## Failure modes (eng-review identified)

| Failure | Test? | Error handling? | User sees |
|---|---|---|---|
| LFO rate=0 (DC mode) | GAP in lfo.test.ts | Outputs 0 (sine(0)=0, etc.) | Tystnad — OK |
| Cable to deleted LFO | GAP in evaluateKnob unit | Returns knob.base (graceful) | Tystnad eller defensiv UI |
| Pace modulated under hw-min | Bounds-clamp covers | clamp till PACE_MIN | Pace clamps visibly |
| Stop mid-emit (CRITICAL) | GAP-B integration test | Generation invalidates | NO late descriptors |
| DC-stim phase=0 (CRITICAL) | GAP-A phase-flip test | 2.1A per-channel flip | Biphasic verkar |
| Two channels same elcon, opposite phase | Existing mock-firmware queue | Sub-queue routing | OK per mock-fw design |
| SeqNr wraps 256 | Trivial test | mask `& 0xff` | Continous |

**Critical gaps if any:** 0 (alla CRITICAL har tests-on-record GAP-A/GAP-B).

## Worktree parallelization strategy

```
Lane A (FOUNDATION — blocks all):
  1. src/synth/types.ts (entities, no logic)
  2. src/protocol/hardware-bounds.ts + tests
  3. src/safety/clamp.ts + relocate clampAmp tests
  4. src/synth/waveforms.ts + tests
  5. src/synth/lfo.ts + tests
  6. src/synth/state.ts + tests (pure data + actions)
  7. src/synth/synth-engine.ts + tests (Clock-interface)

Lane B (parallel after A — UI primitives):
  Three sub-lanes (truly parallel):
   B1. src/ui/synth/Knob.svelte + knob-helpers.ts + tests
   B2. src/ui/synth/MixerChannel.svelte
   B3. src/ui/synth/LFOModule.svelte

Lane C (after B — cable layer):
  8. src/ui/synth/cable-helpers.ts + tests
  9. src/ui/synth/CableLayer.svelte (SVG bezier + drag-cable UX)

Lane D (LAST — composition + integration):
 10. src/ui/synth/synth-store.svelte.ts (Svelte adapter)
 11. src/ui/Mixer.svelte
 12. App.svelte: mutex + Mixer mount
 13. test/integration/mixer-flow.test.ts
 14. Manual smoke + visual QA

Conflict flags: Lane B sub-lanes touch separate Svelte-filer = inga conflicts.
                Lane D modifies App.svelte + ev. stores.svelte.ts (mutex-state).
```

**Parallelization-vinst med 3 worktrees:** Phase 1 (Lane A) sekventiell ~6h.
Phase 2-3 (B+C) ~3h om paralleller. Phase 4 sekventiell ~3h. **Total: ~10-12h
CC** (matchar CEO-plan estimat).

## Spec Review

Skipped — eng-review locks adresserar identifierade risks. /autoplan kan
köras för adversarial review om större risk uppstår.

## Next step

Implementation Phase 1 (Lane A): börja med `src/synth/types.ts` och bygg
synth-core med tester. Använd `/checkpoint` mid-phase för progress-säkring.

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 1 | CLEAR (PLAN) | 8 proposals, 4 accepted, 4 deferred (SELECTIVE EXPANSION) |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 1 | CLEAR (PLAN) | 13 issues, 0 critical gaps |
| Design Review | `/plan-design-review` | UI/UX gaps | 0 | — | not run (recommended for Mixer.svelte UX) |
| Codex Review | `/codex review` | Independent 2nd opinion | 0 | — | not run (skipped per outside-voice ceremony) |
| DX Review | `/plan-devex-review` | Developer experience gaps | 0 | — | n/a (solo project) |

**UNRESOLVED:** 0  
**VERDICT:** CEO + ENG CLEARED — ready to implement. Recommend `/plan-design-review` before final UI polish (Mixer.svelte är UX-tung).
